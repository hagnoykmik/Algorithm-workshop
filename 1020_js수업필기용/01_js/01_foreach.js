// // 1.
const colors = ['red', 'blue', 'green']

// const printClr = function (color) {
//   console.log(color)
// }

// colors.forEach(printClr)

// // 2
// // colors라는 배열의 요소들을 반복을 하면서 각각의 요소들에 함수 로직을 적용시킨다 
// colors.forEach(function (color) {
//   console.log(color)
// })

// 3
colors.forEach((color) => {
  console.log(color)
})